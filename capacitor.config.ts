import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.shreeva.swasti',
  appName: 'Swasti Bookings',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
